<% provide(:title, "Cursos Inscritos") %>
<div class="container">
  <div>
    <ol class="breadcrumb ms">
        <li><%= link_to "Inicio", root_path %></li>
        <li class="active"> Cursos</li>
    </ol>
  </div>
  <div>
    <h1 class="question-instructions">Cursos Inscritos</h1>    

    <table class="table">
      <tr>
        <th>Nombre</th>
        <th>Año</th>
        <th>Semestre</th>
        <th>Alumnos</th>
        <th colspan="3">Operaciones</th>
      </tr>
      <% @courses.each do |course| %>
        <tr>
          <td><%= course.name %></td>
          <td><%=  %></td>
          <td><%= course.semester %></td>
          <td><%= course.students %></td>
          <td><%= link_to 'Ver', course %></td>
          <!--td><%= link_to 'Editar', edit_course_path(course) %></td!-->
          <!--td><%= link_to 'Eliminar', 'javascript:;', :onclick => 'deleteCourseDialog.showDialog("'+course.name+'", '+String(course.id)+');' %></td!-->
        </tr>
      <% end %>
    </table>
    <div>
      <%= link_to 'Volver', root_path, class: "btn btn-primary square" %>
    </div>
  </div>
</div>

<%# Este div contiene el dialogo de confirmacion de los estandares frontend %>
<div data-role="dialog" id="dialog" class="padding20 dialog" data-close-button="true" data-overlay="true" data-overlay-color="op-dark" data-overlay-click-close="true" style="width: auto; height: auto; visibility: visible; left: 817.5px; top: 404.5px;">
            <h1>¿Eliminar Curso?</h1>
            <p>
                ¿Esta seguro que desea eliminar el curso <b id="delete-course-name"></b>?
            </p>
            <%= link_to 'Eliminar', '', method: :delete, class: "btn btn-primary square", id: 'delete-course-button' %></td>
            <%= button_tag 'Cancelar', :onclick => "deleteCourseDialog.hideDialog()", class: "btn btn-primary square" %>
</div>
