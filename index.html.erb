<% provide(:title, "Inicio") %>
<div class="container">
	<div class="grid">	    
		<div class="greetings-container">
			<div class="row">
				<div class="col-md-6">
					<h1>Heurísticas de Nielsen</h1>
					<p>Una aproximación a la Usabilidad Web o ¿Cómo sobrevivir en la Red?</p>
					<%= link_to 'Empieza aquí!', introduction_path, class: "button-banner-cover" %>
				</div>

				<div class="col-md-6">
					<div>	
						<%= image_tag "button_push-256.png", alt: "Usability" %>
					</div>
				</div>
            </div>
	    </div>
	</div>
</div>


<div class="container">
	<div class="row">
		<div class="col-xs-3">
			<%= link_to(heuristics_path) do %>				
				<%= image_tag "heuristics.png", alt: "Heurísticas", class: "icon-anchor" %>
			<% end %>
			<div class="section-title">
				<%= link_to 'Heurísticas', heuristics_path, class: "text-anchor" %>
			</div>
			<p>Conoce las diez guías generales para el diseño de interfaces definidas por el gurú de la usabilidad Jacob Nielsen.</p>
		</div>
		
		<div class="col-xs-3">
			<%= link_to(evaluations_path) do %>
				<%= image_tag "evaluation.png", alt: "Evaluación", class: "icon-anchor" %>
			<% end %>			
			<div class="section-title">
				<%= link_to 'Evaluación', evaluations_path, class: "text-anchor" %>
			</div>
			<p>Mide tus conocimientos a través de breves evaluaciones, entérate de tus debilidades y supera tu propia marca.</p>
		</div>

		<div class="col-xs-3">
			<%= link_to(practices_path) do %>
				<%= image_tag "practice.png", alt: "Práctica", class: "icon-anchor" %>
			<% end %>			
			<div class="section-title">
				<%= link_to 'Práctica', practices_path, class: "text-anchor" %>
			</div>
			<p>Aplica la teoría en la reparación de una interfaz dañada trabajando directamente con las tecnologías de front-end a través de Codepen.</p>
		</div>

		<div class="col-xs-3">
			<%= link_to(references_path) do %>
				<%= image_tag "resources.png", alt: "Recursos", class: "icon-anchor" %>
			<% end %>
			<div class="section-title">
				<%= link_to 'Recursos', references_path, class: "text-anchor" %>
			</div>
			<p>Navega y aprende a través de la colección de referencias externas reunidas en este recurso de aprendizaje.</p>
		</div>
	</div>	
</div>


<%# popup para continuar evaluacion si no se completó vez pasada (todo el contenido del if)
	abajo sigue el script para que solo aparezca una vez por dia por pestaña  %>
<% @evaluations.each do |evaluation| %>
	 <% if current_user %>		
		<% if @continue[evaluation.id] %>

			<div id="popup" class="modal fade" role="dialog">
				<div class="modal-dialog">
					<!-- Modal content-->
					<div class="modal-content">

						<div class="modal-header">
							<button type="button" class="close" data-dismiss="modal">&times;</button>
							<h4>No has terminado tus evaluaciones<br>¿Deseas continuarlas?</h4>
						</div>

						<div class="modal-body">
							<div>
								<%= link_to 'Si', {controller: 'evaluations', action: "continue", evaluation_id: evaluation.id}, class: "btn btn-primary square"  %>
								<button type="button" class="btn btn-secondary square" href="#popup" data-dismiss="modal">No</button>
							</div>								
						</div>

					</div>
				</div>
			</div>

			<script>
				if (!sessionStorage.alreadyClicked) {
					$("#popup").modal();
				    sessionStorage.alreadyClicked = "true";
				}
			</script>


		<% end %>
	<% end %>
<% end %>

<script language="JavaScript">

	var dias = 1 //días a los que caduca la cookie
	if(document.cookie.indexOf('popupillo=false')<0){
	cad=new Date()
	cad.setTime(cad.getTime() + (dias*24*60*60*1000))
	expira="; expires=" + cad.toGMTString()
	document.cookie = "popupillo=false" + expira
	ventana_inicio();
	}
</script>